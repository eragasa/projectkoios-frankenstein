# `applications.calculator`

Owns the immutable `CalculatorIntegrationId` value used by calculator-selectable application registries. Backend-specific modules may declare equal values independently; no global integration-ID singleton is required.

## Public symbols

- `CalculatorIntegrationId`
