# PwDftScfDiagnostic

`code` is a stable diagnostic identifier. `severity` classifies its effect. `message` preserves a human-readable explanation. `native_artifact` identifies the exact supporting calculator evidence.
