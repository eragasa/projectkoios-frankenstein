# `StoichiometricChargeConstraintResolver`

Uses `absolute_tolerance` when checking complete assignments. `resolve`
validates a complete charge assignment or solves exactly one missing
species-charge parameter; it rejects underdetermined systems.
