# `StructureDeclaration`

Mutable structure with a unique `name` and composed `unit_cell` declaration.
`from_stoichiometry` constructs it from a species composition and an imported
PhysKit `DirectLattice3D`. A material system may associate many such structures.
