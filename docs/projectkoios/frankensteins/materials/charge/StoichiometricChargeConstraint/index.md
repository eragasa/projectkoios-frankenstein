# `StoichiometricChargeConstraint`

Immutable normalized linear equality. `scope` identifies the source unit cell,
`terms` preserve separate species-charge parameters, and `target_charge` is the
normalized right-hand side.
