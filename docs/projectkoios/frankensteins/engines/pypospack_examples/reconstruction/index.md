# `pypospack_examples.reconstruction`

**Source:** `src/projectkoios/frankensteins/engines/pypospack_examples/reconstruction.py`

`PypospackExampleReconstruction` is the immutable static artifact.
`reconstruct_example_engine(checkout_root, engine_name)` verifies and records
one exact example without importing or executing historical code.
