# `MgOChargeProbabilityDistributionFactoryObject`

Immutable consumer-owned factory with an enumerated `adapter`. `create` returns
the historical uniform magnesium-charge distribution with bounds `[1.5, 2.5)`.
