# `UnitCellJsonSerializer`

Deterministically serializes a `ConventionalUnitCell` or `PrimitiveUnitCell` to the reviewed unit-cell JSON schema. It records the stable structure ID, nominal representation, column-vector convention, physical `lattice_parameter`, and ordered fractional basis.
