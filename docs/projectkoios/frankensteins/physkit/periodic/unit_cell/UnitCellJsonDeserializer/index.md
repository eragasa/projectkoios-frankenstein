# `UnitCellJsonDeserializer`

`deserialize` validates reviewed unit-cell JSON and constructs the declared `ConventionalUnitCell` or `PrimitiveUnitCell`. Representation selection uses a closed source-controlled mapping and never imports code named by input data.
