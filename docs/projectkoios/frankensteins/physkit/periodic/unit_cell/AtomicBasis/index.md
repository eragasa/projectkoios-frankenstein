# `AtomicBasis`

Immutable ordered basis with the public nonempty `atoms` tuple. Ordering is represented because calculator formats may require deterministic site order. The basis does not own the primitive lattice or calculator policy.
