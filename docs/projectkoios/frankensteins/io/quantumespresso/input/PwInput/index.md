# `PwInput`

Immutable `pw.x` input record. The public `groups` tuple preserves group order and contains only exact `PwInputGroup` values. The record owns no scientific defaults or filesystem behavior.
