# `PwInputGroup`

Immutable ordered group with public fields `kind`, `tag`, and `lines`. A `kind` is `namelist` or `card`; the `tag` retains the namelist or card header; `lines` retains stripped lexical body rows.
