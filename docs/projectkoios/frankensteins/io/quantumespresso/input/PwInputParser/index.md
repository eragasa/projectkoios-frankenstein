# `PwInputParser`

Effect-free parser whose public `parse` action accepts bounded `pw.x` text and returns one immutable `PwInput`. It recognizes Fortran namelists and the documented supported card set while retaining body rows lexically.
