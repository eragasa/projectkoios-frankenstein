# `PwInputWriter`

Effect-free writer whose public `render` action produces deterministic `pw.x` text. It controls whitespace and namelist delimiters only; group order, variables, lexical values, cards, and rows remain caller-owned.
