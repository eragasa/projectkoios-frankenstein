# `PwOutputParser`

The public `parse` action scans bounded `pw.x` standard-output text and constructs one `PwOutput`. Repeated energy, cutoff, pressure, and count observations use their last represented value. Fortran `D` exponents are admitted.
