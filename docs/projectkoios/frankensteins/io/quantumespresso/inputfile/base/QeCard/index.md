# `QeCard`

Nominal immutable base with ordered `lines` and an optional `option`. The derived `kind` and `tag` identify the QE group. `to_input_group` projects the nominal card into the maintained lexical input model.
