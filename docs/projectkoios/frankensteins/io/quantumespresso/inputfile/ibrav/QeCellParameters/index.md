# `QeCellParameters`

Immutable `CELL_PARAMETERS` declaration containing three direct-lattice `vectors`, an explicit QE `unit`, and explicit `coordinate_precision`. Precision is retained as policy because the number of digits required to preserve symmetry is system-dependent.
