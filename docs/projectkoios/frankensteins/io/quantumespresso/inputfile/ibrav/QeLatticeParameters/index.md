# `QeLatticeParameters`

Immutable representation of the `a`, `b`, `c`, `cos_ab`, `cos_ac`, and `cos_bc` parameter family corresponding to QE `A`, `B`, `C`, `cosAB`, `cosAC`, and `cosBC`. Lengths are expressed in Å. `has_shape_parameters` reports whether any value beyond `a` is present.
