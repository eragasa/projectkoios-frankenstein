# `QeTitleCard`

Immutable slotted `QeControlCard` specialization with a typed `title` field. It defaults to the documented single blank character, preserves plain ASCII title content, and derives the quoted lexical assignment.
