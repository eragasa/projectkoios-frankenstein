# `CalculatorExecutionRecord`

Immutable terminal record with public `command`, `working_directory`, `status`, `returncode`, `stdout_filename`, `stderr_filename`, `required_input_filenames`, `error_type`, and `error_message` fields. The JSON representation additionally records `schema_version = 1`.
