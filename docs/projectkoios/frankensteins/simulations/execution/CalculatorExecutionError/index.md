# `CalculatorExecutionError`

Raised only after a nonzero exit, launch failure, or timeout has been written durably. Public `record` and `record_path` attributes provide the same immutable terminal evidence and its JSON location.
