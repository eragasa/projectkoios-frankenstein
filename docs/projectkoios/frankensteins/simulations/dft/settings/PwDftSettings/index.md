# `PwDftSettings`

Immutable calculator-neutral settings selected once on `PwDftSimulation`. The initial model contains the required `calculation_type`; additional typed settings will be added with their unit and projection contracts rather than as arbitrary strings.
