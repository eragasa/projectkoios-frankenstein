# `TagAlignment`

Immutable registry entry with public `tag`, `qe_fields`, `vasp_fields`, `qe_alignment`, `vasp_alignment`, and `qualification` fields. Field names identify calculator input surfaces; alignment kinds and qualifications prevent a name crosswalk from being mistaken for scientific equivalence.
