# QeScfOutputArtifactAnalyzer

`artifact_root` bounds retained evidence. `maximum_artifact_bytes` limits reads. `analyze` returns a normalized observation; `_execution`, `_resolve`, `_read`, `_validate_basename`, and `_ry_to_ev` implement bounded evidence handling.
