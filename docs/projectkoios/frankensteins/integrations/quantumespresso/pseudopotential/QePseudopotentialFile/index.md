# `QePseudopotentialFile`

A nominal `PseudopotentialFile` subtype whose `pseudopotential` must be an exact `QePseudopotential`. It inherits public `filename`, `sha256`, `byte_size`, and read-only `symbol` identity surfaces.
