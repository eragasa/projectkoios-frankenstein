# `QePseudopotential`

A nominal `Pseudopotential` subtype. It inherits the public `symbol`, `exchange_correlation`, `formalism`, `relativistic_treatment`, and `valence_electrons` fields and adds the represented `upf_version`.
