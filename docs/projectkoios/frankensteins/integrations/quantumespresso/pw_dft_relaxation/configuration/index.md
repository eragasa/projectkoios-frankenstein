# QE relaxation projection configuration

Declares required QE-native species, optimizer, optional cell controls, cutoff ratio, electronic threshold, path values, filename, and rendering precision. No numerical defaults are supplied.

## Public symbols

- `QeRelaxationProjectionConfiguration`
