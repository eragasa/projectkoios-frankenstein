# `projectkoios.frankensteins.integrations.quantumespresso.outputs.dos`

`dos.x` output composition:

- `QeDosFile`
- `QeDosFileParser`
- `QeDosFileResult`

Parsing remains an explicit `NotImplementedError` stub.
