# `projectkoios.frankensteins.integrations.quantumespresso.outputs.projected_dos`

`projwfc.x` projected-DOS composition:

- `QeProjectedDosFile`
- `QeProjectedDosFileParser`
- `QeProjectedDosFileResult`

Parsing remains an explicit `NotImplementedError` stub.
