# `VaspCalculationProjection`

Immutable result with public `input_file`, `alignment`, `required_inputs`, and `qualification` fields. `is_complete` is true only when this calculation-mode projection has no unresolved inputs. It does not assert that the complete VASP input set is runnable or scientifically converged.
