# VASP SCF action handler

`VaspPwDftScfTask` binds workflow identities to a pre-staged execution. `VaspPwDftScfActionHandler` composes the common handler contract with `VaspScfIntegration` and `CalculatorExecutor`.
