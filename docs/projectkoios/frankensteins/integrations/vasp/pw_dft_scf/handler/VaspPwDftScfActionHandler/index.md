# VaspPwDftScfActionHandler

`integration`, `tasks`, and `executor` are composed dependencies. `register`, `submit`, and `analyze` implement the common action-handler contract. Execution is pre-staged, bounded, shell-free, and recorded by `CalculatorExecutor`.
