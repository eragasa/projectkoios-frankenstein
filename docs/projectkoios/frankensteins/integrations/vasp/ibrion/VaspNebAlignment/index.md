# `VaspNebAlignment`

Immutable workflow alignment containing `vasp_fields`, `qe_executable`, `qe_fields`, `alignment`, and `qualification`. VASP NEB is enabled by `IMAGES` and `SPRING` with a separately selected optimizer; QE uses `neb.x` and `&PATH`.
