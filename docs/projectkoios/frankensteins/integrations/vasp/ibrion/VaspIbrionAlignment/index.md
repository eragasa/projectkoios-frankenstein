# `VaspIbrionAlignment`

Immutable record containing `ibrion`, `purpose`, `qe_executable`, `qe_fields`, `alignment`, `algorithm_equivalent`, and `qualification`. It prevents a shared purpose from being reported as algorithm identity.
