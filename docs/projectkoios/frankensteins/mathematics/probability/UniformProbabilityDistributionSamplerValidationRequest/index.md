# `UniformProbabilityDistributionSamplerValidationRequest`

Immutable `ValidationRequest` containing the uniform `sampler`, `sample_count`,
`random_seed`, `bin_count`, and `standard_error_limit` needed for deterministic
statistical validation.
