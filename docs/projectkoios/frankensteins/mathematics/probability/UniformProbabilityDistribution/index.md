# `UniformProbabilityDistribution`

Uniform probability-distribution composition. Construction receives uniform
`parameters` and an enumerated `adapter`, creates the corresponding
`UniformProbabilityDistributionSampler`, and ensures both objects share the
same parameters identity.
