# VASP k-point convergence

`campaign.toml` declares a calculator-neutral cubic k-point sequence at fixed wavefunction cutoff and selects the stable `vasp` integration. Execution and durable result storage remain external to the convergence assessor.
