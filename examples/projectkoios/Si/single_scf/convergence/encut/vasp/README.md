# VASP ENCUT convergence

`campaign.toml` varies the calculator-neutral wavefunction cutoff at fixed cubic k-point density and selects VASP. `encut` is the example-path label; the common application model remains calculator-neutral.
