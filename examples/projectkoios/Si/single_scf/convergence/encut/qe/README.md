# Quantum ESPRESSO wavefunction-cutoff convergence

`campaign.toml` uses the same eV coordinates and convergence policy as the VASP example. Equal numeric cutoff coordinates do not establish equal basis quality across pseudopotentials or calculators.
