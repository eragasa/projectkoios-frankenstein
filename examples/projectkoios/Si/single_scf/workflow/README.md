# Shared single-SCF workflow

`dft_pw_scf/` contains the optional example-local SNAKES realization of the maintained calculator-neutral child-workflow contract. `runner/` owns configuration resolution and external orchestration. Neither calculator backend defines a separate net.
