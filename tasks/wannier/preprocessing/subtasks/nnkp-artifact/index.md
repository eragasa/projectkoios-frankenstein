# Subtask: `.nnkp` artifact contract

Declare bounded `.nnkp` identity and the supported k-point/neighbor observations required for interface validation.

**Acceptance:** raw bytes are retained and unsupported sections do not receive speculative semantics.
