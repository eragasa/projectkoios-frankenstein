# Subtask: `.win` declaration

Define immutable fields for functions, bands, projections, k points, windows, convergence controls, and requested outputs.

**Acceptance:** scientific values have no hidden defaults at the maintained boundary.
