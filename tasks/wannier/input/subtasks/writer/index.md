# Subtask: deterministic `.win` writer

Render one accepted declaration into normalized Wannier90 text.

**Acceptance:** equal declarations produce equal bytes and unsafe path or multiline values are rejected.
