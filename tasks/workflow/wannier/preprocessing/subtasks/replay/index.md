# Subtask: preprocessing workflow replay

Replay retained `.win` and `.nnkp` evidence through the concrete handler.

**Acceptance:** replay produces the same qualified terminal result without tool execution.
